export default interface IProduct {
    // tag:string
    quantity: number
    articleNumber: string
    name: string
    price: number
    description?: string
    imageName: string
    category: string
}
